# Быстрый старт Chemistry Bot

## 1️⃣ Предварительная подготовка

### Получите токен бота
1. Откройте Telegram и напишите `@BotFather`
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и скопируйте полученный токен

### Определите ваш User ID
1. Напишите `@userinfobot` в Telegram
2. Скопируйте ваш User ID

## 2️⃣ Инсталляция на macOS

```bash
# Перейдите в папку проекта
cd /Users/caprize/Desktop/avr_bot

# Запустите скрипт установки
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- Установит Redis (если не установлен)
- Создаст виртуальное окружение Python
- Установит все зависимости

## 3️⃣ Конфигурация

Отредактируйте файл `config.json`:

```json
{
  "bot_token": "YOUR_BOT_TOKEN_HERE",
  "admin_ids": [YOUR_USER_ID_HERE],
  "redis": {
    "host": "localhost",
    "port": 6379,
    "db": 0
  },
  "lectures_storage": "./lectures"
}
```

Пример:
```json
{
  "bot_token": "1234567890:ABCdefGHIjklmnoPQRstuvWXYZ",
  "admin_ids": [123456789],
  "redis": {
    "host": "localhost",
    "port": 6379,
    "db": 0
  },
  "lectures_storage": "./lectures"
}
```

## 4️⃣ Проверка подключения к Redis

```bash
source venv/bin/activate
python3 -c "import redis; r = redis.Redis(); r.ping(); print('✅ Redis OK')"
```

Если вывод: `✅ Redis OK` - всё работает!

## 5️⃣ Запуск бота

```bash
source venv/bin/activate
python bot.py
```

Вы должны увидеть сообщение:
```
2026-02-07 12:00:00 - telegram.ext._updater - INFO - Bot started polling
```

## 6️⃣ Использование администраторского CLI

В отдельном терминале:

```bash
cd /Users/caprize/Desktop/avr_bot
source venv/bin/activate
python admin_cli.py
```

### Команды CLI:
- **1** - Добавить ученика
- **2** - Просмотреть данные ученика
- **3** - Список всех учеников
- **4** - Обновить расписание ученика
- **5** - Добавить лекцию ученику
- **6** - Удалить лекцию у ученика
- **7** - Добавить лекцию в систему
- **8** - Просмотреть лекцию
- **9** - Список всех лекций
- **10** - Удалить лекцию
- **11** - Удалить ученика
- **12** - Статистика БД
- **13** - Очистить всю БД
- **0** - Выход

## 7️⃣ Тестирование бота в Telegram

1. Откройте Telegram и найдите вашего бота по имени
2. Отправьте `/start`
3. Вы должны увидеть меню администратора

### Пример сценария:

**Как администратор:**
1. Нажмите "➕ Добавить ученика"
2. Введите: `student_username пн,ср,пт 15:00-16:00`
3. Нажмите "📚 Добавить лекцию"
4. Введите название лекции
5. Отправьте файл лекции
6. Нажмите "✏️ Редактировать ученика"
7. Выберите ученика и добавьте ему лекцию

## 8️⃣ Добавление нового ученика

```bash
python admin_cli.py
# Выберите 1 (Add student)
# Введите user_id, username и расписание
```

**Или через Telegram:**
- Администратор нажимает "➕ Добавить ученика"
- Вводит: `@student_username пн,ср,пт 15:00-16:00`

## 9️⃣ Добавление лекции

```bash
python admin_cli.py
# Выберите 7 (Add lecture)
# Введите название и путь к файлу
```

## 🔟 Остановка и перезапуск

### Остановить бота
```bash
Ctrl+C в терминале с ботом
```

### Остановить Redis
```bash
redis-cli shutdown
```

### Перезапустить Redis
```bash
redis-server --daemonize yes
```

## 🔍 Устранение неполадок

### Ошибка: Redis connection refused
```bash
# Проверьте, запущен ли Redis
redis-cli ping

# Если не работает, запустите Redis
redis-server --daemonize yes
```

### Ошибка: Invalid bot token
- Проверьте, правильно ли вы скопировали токен в config.json
- Убедитесь, что нет пробелов в начале/конце

### Ошибка: Import redis could not be resolved
```bash
source venv/bin/activate
pip install -r requirements.txt
```

## 📝 Полезные команды

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Деактивировать
deactivate

# Проверить установленные пакеты
pip list

# Запустить тесты базы данных
python test_database.py

# Просмотреть логи Redis
redis-cli MONITOR

# Получить статистику Redis
redis-cli INFO
```

## 🚀 Готово!

Бот готов к использованию. Все функции доступны через интерфейс Telegram!

Для развертывания на сервере см. `PROJECT_README.md`
